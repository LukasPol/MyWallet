<nav class="navbar navbar-expand-lg bg-white">
  <div class="container-fluid px-0">
    <a class="navbar-brand" href="#"><%= t(:name_app) %></a>
    <% if current_user.present? %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to wallets_path, class: "nav-link #{active_when(['wallets'])}" do %>
              <%= t(:wallet, scope: 'navbar') %>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to tradings_path, class: "nav-link #{active_when(['tradings'])}" do %>
              <%= t(:tradings, scope: 'navbar') %>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to destroy_user_session_path, data: {"turbo-method": :delete, confirm: t(:are_you_sure)}, class: "nav-link" do %>
              <%= t(:logout, scope: 'navbar') %>
            <% end %>
          </li>
        </ul>
      </div>
    <% else %>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "nav-link active" do %>
              <%= t(:login, scope: 'navbar') %>
            <% end %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>